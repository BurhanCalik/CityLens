# CityLens ML pipeline dependencies.
#
# torch is intentionally NOT pinned here: install the build that matches your
# platform/CUDA first (see https://pytorch.org/get-started/locally/).
# On the hackathon machine torch + deface are already installed.

transformers>=4.44.0
timm>=1.0.0
pillow>=10.2.0
requests>=2.31.0
python-dotenv>=1.0.1

# Face anonymization (KVKK). Already installed on the hackathon machine.
deface>=1.5.0
